# Port the function runs on (defaults to 8080 if not set)
PORT=8080

# Comma-separated list of health check endpoints to monitor
HEALTH_CHECK_ENDPOINTS=https://api1.example.com/health,https://api2.example.com/health

# Optional: per-request timeout in milliseconds (omit for no timeout)
# HEALTH_CHECK_TIMEOUT_MS=5000
